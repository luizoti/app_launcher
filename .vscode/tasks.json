{
    "tasks": [
        {
            "command": "${cwd}/.venv/bin/python ${cwd}/main.py",
            "group": {
                "isDefault": true,
                "kind": "build"
            },
            "label": "Run P${cwd}",
            "problemMatcher": [],
            "type": "shell"
        }
    ],
    // See https://go.microsoft.com/fwlink/?LinkId=733558
    // for the documentation about the tasks.json format
    "version": "2.0.0"
}